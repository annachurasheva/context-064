# Сводный анализ исследования портала OK (Одноклассники)

## Общая информация

**Портал:** ok.ru
**Заголовок страниц:** «Одноклассники»
**Дата инспекции:** 11.09.2026
**Инструмент:** `InspectOK-DOM.ps1` (PowerShell 7, CDP WebSocket)
**Профиль инспекции:** user_01
**Задача:** TASK-0302
**Версия скрипта:** 2.1.0
**Порт CDP:** 9224
**Параметр `guarded_access`:** true

---

## ⚠️ Статус исследования (обязательно к прочтению)

Инспекция портала ОК в рамках TASK-0302 **признана неудачной (FAIL, архив)**.

Во всех трёх прогонах скрипт выдал критическое сообщение:
> `ВНИМАНИЕ! Не удалось получить корень DOM. Секции DOM будут failed.`

Заполнено только **2 из 15** секций отчёта (`01-PAGE`, `15-NOTES`); все DOM-секции (`02-SELECTED_ENTITY` … `14-SELECTORS`) остались в статусе `pending` с пустыми данными.

**Следствие:** приведённые ниже сведения о селекторах и структуре DOM отсутствуют в исходном файле. Ниже зафиксировано только то, что действительно подтверждено файлом. Строки селекторов для анкеты ОК не приводятся сознательно — они должны быть добыты точечными проверками F12 на живых страницах (правило №0 проекта).

---

## 1. Типы страниц и сущностей

### 1.1 Подтверждённые типы страниц (по URL из секции 01-PAGE)

| Тип страницы | URL-образец (из файла) | Источник |
|-------------|------------------------|----------|
| Страница профиля | `https://ok.ru/profile/597269008402` | прогон `profile_page` |
| Страница группы | `https://ok.ru/group/55367549321453` | прогон `group_page` |
| Дискуссия / комментарий автора | `https://ok.ru/discussions/all/1/70000043118027/158192326669771` | прогон `comments_author` |

Выводы из образцов:
- Профиль идентифицируется путём `/profile/{id}`;
- Группа идентифицируется путём `/group/{id}`;
- Дискуссия/комментарий имеют путь `/discussions/{раздел}/{топик}/{сообщение}` (глубокая ссылка на сообщение).

> Остальные типы страниц (упоминание, нить ЛС, товар, фото) в данном файле **не подтверждены** — см. `OK_ANALYSIS_GAPS.md`.

### 1.2 Типы сущностей

В файле не определено. В шапке отчёта `ENTITY_TYPE` = `unknown`, `ENTITY_NAME` = `auto_detected`. Данные отсутствуют.

---

## 2. Структура DOM и селекторы

**Данные отсутствуют.** Все соответствующие секции инспекции (`03-ENTITY_CANDIDATES`, `04-EVENT_CONTAINER`, `14-SELECTORS`, `13-RAW_HTML`) имеют статус `pending` и `data: null`.

Подтверждённые факты из файла:
- Селекторы имени автора — **не собраны**.
- Контейнер события — **не собран**.
- Хэш/классы разметки — **не собраны**.

---

## 3. Поведение страницы (Page Behavior)

**Данные отсутствуют.** Секция `10-PAGE_BEHAVIOR` имеет статус `pending`.

Известно лишь из образцов URL (секция `01-PAGE`): существуют маршруты `/profile/`, `/group/`, `/discussions/…`. Вопросы SPA/динамической загрузки/History API в файле не раскрыты.

---

## 4. Извлечение даты/времени

**Данные отсутствуют.** Секция `06-DATE_TIME` имеет статус `pending`.

---

## 5. Специфичные сценарии

**Данные отсутствуют.** В файле нет подтверждённых данных о вложенных комментариях, пермалинках, нитях ЛС, товарах.

> Примечание: расширение не используется на мобильных устройствах; мобильные представления не рассматриваются.

---

## 6. Конфигурация для адаптера

Конфигурация **не может быть сформирована** из-за отсутствия DOM-данных.

Единственный подтверждённый факт для будущей анкеты:
```javascript
// Портал
urls: ["ok.ru"]           // подтверждено образцами /profile/, /group/, /discussions/
```
Поля `queryaz`, `username`, `eventype` и прочие — **не определены** (нет исходных данных).

---

## 7. Рекомендации по дальнейшим действиям

1. Перезапустить инспекцию с работоспособным получением корня DOM (устранить причину «Не удалось получить корень DOM»).
2. Использовать ручные точечные проверки F12 (по чек-листу из `connecting_a_new_portalinstruction.md`) для подтверждения селекторов.
3. Дополнительно исследовать типы страниц: упоминание, нить ЛС, товар, фото (список пробелов — в `OK_ANALYSIS_GAPS.md`).

---

## 8. Сравнение с эталоном VK.RU

| Аспект | VK.RU (собран) | OK (текущий статус) |
|--------|----------------|---------------------|
| Селекторы имени автора | собран (`data-testid`) | **не собран** |
| Контейнер события | собран | **не собран** |
| Даты | собран | **не собран** |
| Типы страниц | собраны (6+) | подтверждены 3 (профиль, группа, дискуссия) |
| Поведение страницы | собран (React, SPA) | **не собран** |
| Shadow DOM | не используется | **не проверен** |

---

## 9. Тестовые URL (подтверждённые образцами из файла)

```
https://ok.ru/profile/597269008402
https://ok.ru/group/55367549321453
https://ok.ru/discussions/all/1/70000043118027/158192326669771
```

---

## 10. Заключение

Файл `ok_ru_mbuktskska.txt` подтверждает **только URL-структуру трёх типов страниц** портала ОК (`/profile/`, `/group/`, `/discussions/`) и метаданные прогона. Все данные о DOM, селекторах, событиях, датах и поведении страницы **отсутствуют** из-за сбоя получения корня DOM в ходе инспекции (TASK-0302 = FAIL).

Анкета портала ОК **не может быть заполнена** на основе текущего файла. Необходим повторный сбор подтверждённых селекторов (полный список пробелов — в документе `OK_ANALYSIS_GAPS.md`).