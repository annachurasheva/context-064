***

## TASK‑0300 · 09.10.2026

**Коммит:** `64a5aaf`

**Суть задачи:**

Ошибка - переиздать

***

## TASK‑0301 · 09.11.2026

**Коммит:** `08f0f20`

**Суть задачи:**

Ошибка - отдача содержала .gitignore и package-lock.json вне разрешённого списка; доотдача TaskRegistry.tsx запрошена

***

TASK-0301 · 09.11.2026

**Коммит:** `08f0f20`

Дельта — подтверждает PASS.

***

## TASK-0302 (этап 2, правка 1) · 11.09.2026

**Коммит:** `b2efb6f`

**Суть задачи:**

Реализован разбор сообщений CDP с учётом `Set-StrictMode`: проверка свойств через `$Obj.PSObject.Properties.Name`, игнорирование событий (field `method`), ожидание ответа по `id`. Каждый вложенный доступ (`result.root.nodeId`) защищён (guarded); отсутствие поля переводит статус секции в `failed` с нотой, не прерывая скрипт.
Рукопожатие WebSocket остаётся критическим: неудача → СТОП с ошибкой.

Образцы сценариев расширены:
- `profile_page`: `/profile/` ИЛИ слаг `имя.число{6,}`.
- `group_page`: `/group/` ИЛИ `group[0-9]` ИЛИ односложный слаг без точки-числа (исключая зарезервированные).
- `comments_author`: `/discussions/` ИЛИ `/topic/`.
Параметр `-UrlFilter` работает как ручное перекрытие. Несовпадение → СТОП с подсказкой.

Сценарий `comments_author` собирает структурно: контейнер = блок с ссылкой-автором, текстом, датой; автор = первая ссылка; текст = `innerText` минус шум. Найденные селекторы и счётчики → секция [14].

Статусы секций [02]–[14]: `complete` / `failed` (с нотой) по факту сбора. Ключи 15 секций неизменны. Отчёт пишется всегда (даже частичный).
Версия скрипта: 2.1.0.

***