# TASK-0151: тест маркировки ОК (всё готово, одно движение)

```
1. pwsh scripts/Build.ps1                 # dist/ свежий
2. .\scripts\Run-user_01.ps1              # Play в VS Code тоже;
                                          # порт 9226, профиль чистый
3. В открывшемся Edge: ok.ru → ОДНА сохранённая персона → F5
4. edge://extensions → карточка «Записки ОК» → ссылка
   «Служебная страница» (нет её — «Подробные сведения» →
   «Просмотреть представления») → Console открыта
5. ДВИЖЕНИЕ: ПКМ по персоне → «Записка: ПЕР/СОО»
6. ПРОВЕРКА: Console «saved card …» · badge зелёный «1» ·
   карточка в картотеке (NAME_HINT не тронут)
7. ОТЧЁТ: да/нет + строки лога → дашборд (4-е движение)
   и logs/activity. Нет — лог мне/Кодеру, ≤2 итераций.
```

=======
1. pwsh scripts/Build.ps1          # dist/ = артефакт
   pwsh scripts/RunEdgeCdp.ps1 -Profile user_01   # порт 9226
   edge://extensions → Load unpacked → mem-OK/dist
o     k.ru: одна сохранённая персона → ПКМ «Записка: ПЕР/СОО»
   Проверка: SW-лог «saved card…», badge зелёный «1», карточка
      в картотеке (NAME_HINT не тронут). Отчёт: дашборд (4-е движение)
+ logs/activity. Нет — лог Кодеру, ≤2 итераций.
3. Открыть ok.ru: ОДНА сохранённая персона (ваша тестовая или
   публичная страница персоны). Чужая территория — чтение.
4. ДВИЖЕНИЕ (единственное): ПКМ по персоне →
   «Записка: ПЕР/СОО».
5. ПРОВЕРКА (три точки):
   а) SW-консоль: лог «saved card …» (правило единственного
      писателя);
   б) badge: зелёный «1»;
   в) картотека (ui): карточка на месте, displayName не тронут
      (NAME_HINT).
6. ОТЧЁТ: да/нет + строки лога; положить в дашборд (4-е движение)
   и logs/activity (запись v4: кто·когда·что·результат).