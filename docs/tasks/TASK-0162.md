1) восстановить: git checkout ed13e0c -- EdgeExtension/manifest.json
   (после — pwsh scripts/Build.ps1 должен пройти);
2) добавить 5 обёрток scripts/Run-{s_admin,admin,moderator,editor,
   user_01}.ps1 (коммент-имя + & RunEdgeCdp.ps1 -Profile X);
3) tasks.json валидный: "version":"2.0.0","tasks":[ Build
   (scripts/Build.ps1), Edge CDP, Build+Run (dependsOn,
   dependsOrder sequence) ];
4) settings.json: .edge-cdp-profile → .edge_test_* / Edge_test_ok;
5) RunEdgeCdp: вернуть фолбэк из трёх путей msedge.exe.
Коммит: fix: манифест восстановлен, обёртки, валидный tasks.json
[TASK-0162]. Отчёт: сделано/осталось + «Build.ps1 прошёл».

TASK-0162 доп.: в scripts/RunEdgeCdp.ps1 переставить param ПЕРВЫМ:
сразу после #Requires; Set-StrictMode и $ErrorActionPreference —
ПОСЛЕ param. Верный верх файла:

# RunEdgeCdp.ps1 — чистый Edge c CDP для тестов ok.ru.
#Requires -Version 7.0
param(
    [ValidateSet("s_admin","admin","moderator","editor","user_01")]
    [string]$Profile = "user_01"
)
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'