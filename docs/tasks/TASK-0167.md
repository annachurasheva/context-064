TASK-0167 (общий план фиксов):
1) RunEdgeCdp.ps1: после создания профиля, ДО Start-Process —
   выставить developer_mode = true в <ProfileDir>\Default\Preferences
   (ключ extensions.ui.developer_mode); Preferences существует —
   точечно дополнить, остальное не перезаписывать; не существует —
   создать минимальный json;
2) EdgeExtension/manifest.json: проверить наличие "action": {}
   (пост разведчика v07g); отсутствует — вернуть строкой;
3) в отчёте: Build.ps1 прошёл, консоль SW без красного.
Коммит: fix: developer mode по умолчанию + пост разведчика [TASK-0167].
