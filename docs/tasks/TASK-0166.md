1) ui/kartoteka: кнопка «Экспорт» — chrome.downloads.download
   (data:URL, имя ok-export-<yyyymmdd-hhmm>.json); manifest +=
   "downloads";
2) Import-OkExport.ps1: $date задать ДО использования; валидация:
   Path — файл json (не папка); копия в data/incoming/; отчёт;
3) в шапку Import-OkExport.ps1 — SYNOPSIS-инструкция: «файл
   берётся: кнопка Экспорт в картотеке ИЛИ Console SW → Save as;
   Path = путь к этому файлу».
Коммит: fix: экспорт ОК — кнопка, downloads, Import-OkExport
[TASK-0166]. Отчёт: сделано/осталось.