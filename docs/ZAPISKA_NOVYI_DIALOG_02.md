Записка готова. Разнесите её в репо файлом `docs/ZAPISKA_NOVYI_DIALOG_02.md` (коммит `docs:`), в новом окне (инкогнито) вставьте первым сообщением вместе с хэшем её коммита.

---

# ЗАПИСКА-ПРИГЛАШЕНИЕ В НОВЫЙ ДИАЛОГ №2 (продолжение трека «Анкета-OK»)

**Носитель:** Anna-Ch (руководитель). Вставить первым сообщением нового диалога.
**Рабочая ветка:** `qwen-develop` · репозиторий `annachurasheva/context-064`.
**Основание:** продолжение трека «Анкета-OK» (8-й адаптер dmiandr/context); контекст диалога №1 исчерпан.
**Точка синхронизации:** HEAD `9082fa2`; предшествующие коммиты разноса: `41d57be`, `950376e`, `36b21a6`.

## 0. Текущее состояние (факты только из репо)
- **TASK-OK-001 — DONE:** raw-сверка Дельты выполнена (коммит `bf378bc`), отчёт `reports/TASK-OK-001-REPORT.md`.
- **TASK-OK-002 — В РАБОТЕ:** отдача Кодера получена (полные код-блоки с заголовками «Файл:»), разнос Anna-Ch выполнен коммитами `41d57be` (удалён `dashboard/package-lock.json`), `950376e` (чистка, перенос в архив), `36b21a6` (мусор dashboard), `9082fa2` (синхронизация). **Raw-сверка Дельтой и вердикт PASS/FAIL НЕ выполнены — это первая работа нового диалога.**
- Нулевая строка `docs/answers/answers/CODER-LOG.md` в силе: «Разнос файлов в GitHub выполняет Anna-Ch. Сделанное в песочнице Кодера не является исполнением. Приёмка = файлы в репо + верификация Дельтой.»
- Действующие регламенты: `docs/INSTRUCTION.md` v5, `docs/REGLAMENT-CODER.md` v4.
- Старые хвосты (TASK-0174…0176, «Слон», `t2.md`, `REPORT_OK_ANQUETA.md`, blog-копия, старый чат «Анализ архитектуры и регламен…» под аккаунтом Алексей Колесник) — архив/анулировано; **не продолжать старые сессии, работать только из этой записки.**

## 1. Правило №0 и границы (не пересматривать)
- ИИ сама не напишет ничего: анкета `okcom.js` заполняется только по фактам живой разметки; нет факта — стоп и вопрос.
- Десктоп `ok.ru` (Microsoft Edge); мобильные представления не исследуются; `m.ok.ru` — только запасной источник селекторов при фиаско на основном домене.
- Один ток — один шаг — один отчёт. Память в репо, не в чате.

## 2. Роли и метод (не пересматривать)
- **Anna-Ch:** смыслы, коммиты, тесты, вердикт PASS/FAIL; единственный, кто коммитит (main защищён).
- **Кодер (coder.qwen.ai):** задачи со статусом NEW; отдача — полные тексты файлов (Code Tool Record → Diff → Copy либо полные код-блоки с заголовком «Файл:» по INSTRUCTION v5); не коммитит; readback обязателен.
- **Дельта:** задания, решения, верификация raw на GitHub, печать; не пишет код за Кодера; важное из чата обязано оказаться в репо.
- «Сборка зелёная у Кодера» — НЕ приёмка. Кнопки «Publish to GitHub» / «Опубликовать на Github» в интерфейсе Кодера — не нажимать.

## 3. Интерфейс Code Tool (памятка по каналам и браузерам)
- Интерфейс может быть на русском: **«Разница» ≡ Diff**, «Предварительный просмотр» ≡ Preview, «Инструмент записи кода» ≡ Code Tool Record, «Копировать» ≡ Copy.
- Вкладка Diff активна только при непустом Record; тултип «No code changes to show yet» = Record пуст, это не блокировка.
- Переключение в Diff — ручной клик Anna-Ch; Кодер панель программно не переключает (подтверждено пробой `DIFF-PROBE.md`: вкладки Diff, Log есть; копирование через Diff — да).
- Нет вкладки Diff в данном окне — отдача полными код-блоками допустима (INSTRUCTION v5), приёмке не мешает.

## 4. Обязательный первый шаг нового диалога (Дельте)
1. Прочитать: эту записку; `docs/INSTRUCTION.md` (v5); `docs/REGLAMENT-CODER.md` (v4); `docs/tasks/TASK-OK-002.md` (паспорт + белый список); `docs/answers/answers/CODER-LOG.md` (нулевая строка); `reports/TASK-OK-002-REPORT.md`.
2. Raw-сверка коммитов `41d57be`, `950376e`, `36b21a6`, `9082fa2`: в `dashboard/src/data/` (tasks.ts, tasks-ok.ts, v01.ts) нет хвостов прошлых итераций, шаги трека OK со статусом NEW; нулевая строка CODER-LOG не тронута; старьё вынесено из рабочих папок.
3. Вердикт PASS/FAIL по TASK-OK-002; при PASS — паспорт TASK-OK-003 (Подготовка среды / Полигон, ручной F12) со статусом NEW и белым списком.
4. Команда Кодеру: «возьми обнову по коммиту [хэш вердикта] — читай и делай»; ждать readback.

## 5. Белый список чтения docs/ (действует до расширения решением Дельты/Anna-Ch)
`docs/qwen_01.md`; `docs/REGULATION_OK_COLLECTION_V01.md`; `docs/INSTRUCTION_OK_ATOMS_A1_A6.md`; `docs/OK_ANALYSIS_GAPS.md`. Всё остальное в docs/ — не читать, не цитировать, не переносить в отчёты.

## 6. Правило фриза
Три и более правки одной задачи подряд, уход от узловой задачи, усталость архитектора — СТОП и новый диалог с запиской. Настоящая записка — штатное продолжение узла, не хвостов.

---

*Записка открывает новый диалог с узла: состояние = TASK-OK-002 ждёт raw-сверки; следующий шаг = вердикт и паспорт TASK-OK-003. Ручной режим F12 (ввод кода Дельты руками, экранные перетаскивания) сохраняется за окном-носителем.*