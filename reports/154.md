Готово — TASK-0154. Скрипт сборки сбалансирован (5/5 here-strings), основная сборка зелёная. Подготовлен самодостаточный скрипт, который копирует ядро от донора и адаптирует его к ok.ru.

## Сделано

`mem-OK-prep/scripts/assemble-edgeextension.ps1`— самодостаточный сборщик (копируется в`mem-OK/scripts/`, запускается в локали):

1. КОПИЯ ядра v07g из context-vkru — КАК ЕСТЬ, SHA256 provenance:`messaging.js`·`normalize.js`·`storage.js`·`ui/kartoteka.html`·`ui/kartoteka.js`·`ui/layer.js`·`ui/dialog.js`·`styles.css`(+ исходные manifest/content/background как база). Ядро — единственный писатель, NAME_HINT, badge —НЕ трогается.

2. АДАПТАЦИЯ к ok.ru — заменены ТОЛЬКО 3 файла(ок-варианты встроены в скрипт):

- `manifest.json`→`matches: ["https://ok.ru/*"]`,`host_permissions: ok.ru`, имя «Записки ОК», версия 0.1.0; js-порядок ядра сохранён.
- `content.js`→ точка входа для ok.ru: индекс по картотеке, стекло (`CTX_LAYER`),`CTX_SYNC`,`CAPTURED`+`NAME_HINT`(имя из первого якоря); ok-ид из`profile/N`/`group/N`.
- `background.js`→ ПКМ«Записка: ПЕР» / «Записка: СОО»(contexts: link) → изъятие linkUrl → дедуп по`ok-profile:N`/`ok-group:N`→ запись в картотеку (единственный писатель`loadDb→mutate→saveDb`) → зелёный badge с числом карточек →`NAME_HINT`(guard: только если пусто) →`CAPTURED`контенту.

3.`scripts/RunEdgeCdp.ps1`— ок-профили и порты:`s_admin 9222`·`admin 9223`·`moderator 9224`·`editor 9225`·`user_01 9226`(профили`.edge_test_*`).

4.`docs/TEST_PROTOCOL.md`— приёмка: ПКМ «Записка», дедуп, единственный писатель, NAME_HINT/badge, «НЕ тронуты», RunEdgeCdp.

5. Отчёт`reports/assemble-edgeextension.md`— provenance-таблица SHA256 +цепочка TASK-0002 (context-vkru) → TASK-0100 (m3) → TASK-0154 (mem-OK).

Защита:VK-вариант у донора (context-vkru) не изменяется (копируем, не правим); существующие файлы не перезаписываются; ядро копируется КАК ЕСТЬ.

## Осталось (за пределами скрипта)

- Запуститьв локали:`pwsh scripts/assemble-edgeextension.ps1`— скрипт не запускался в моей среде (нет доноров/PowerShell), поэтомуSHA256 provenance появятся в отчёте после вашего запуска— пришлите их, я сверю.
- Коммит:`feat: mem-OK — EdgeExtension и RunEdgeCdp от донора (provenance) [TASK-0154]`.
- Ручная приёмкана ok.ru по`TEST_PROTOCOL.md`(ПКМ «Записка: ПЕР/СОО», дедуп, badge).

НЕ включено (по заданию): журналы-копии строительства, контур джунов, GRANT, эксплуатационный дашборд.

Подготовлен скрипт`assemble-edgeextension.ps1`для сборки EdgeExtension из донора context-vkru с адаптацией к ok.ru. Ядро v07g копируется без изменений (provenance SHA256), адаптируются только manifest.json, content.js и background.js под ok.ru (ПКМ «Записка: ПЕР/СОО», запись в картотеку, дедуп, NAME_HINT, badge). Добавлен RunEdgeCdp.ps1 с ок-профилями и портами 9222–9226, docs/TEST_PROTOCOL.md, отчёт с provenance-цепочкой TASK-0002 → TASK-0100 → TASK-0154. VK-вариант донора не трогается.

Осталось: запустить скрипт в локали (появятся SHA256), закоммитить`feat: mem-OK — EdgeExtension и RunEdgeCdp от донора (provenance) [TASK-0154]`, провести ручную приёмку по TEST_PROTOCOL.md.