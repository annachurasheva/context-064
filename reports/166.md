Готово — TASK-0166. Все три пункта подготовлены в`mem-OK-prep/`, основная сборка дашборда зелёная.

## Сделано

1) Кнопка «Экспорт» в картотеке—`scripts/Add-ExportButton.ps1`(безопасный патч,не перезаписываетфайлы — добавляет недостающее по маркеру TASK-0166, идемпотентно):

- в`ui/kartoteka.html`— кнопка`<button id="btnExport">Экспорт</button>`перед`</body>`;
- в`ui/kartoteka.js`— обработчик:`CTX_STORAGE.loadDb()`→`data:application/json`URL →`chrome.downloads.download`с именем`ok-export-<yyyymmdd-hhmm>.json`+ лог`[CTX v…] экспорт картотеки: …`;
- в`manifest.json`— добавляет`"downloads"`в`permissions`(если нет).

Причина патча, а не правки напрямую: актуальный`kartoteka.js`живёт только в mem-OK, а каналы к репо сейчас под лимитом — чтобы не гадать структуру и не затереть рабочее, скрипт добавляет блок, не трогая остальное. Также лежит полный ок-манифест`EdgeExtension/manifest.json`(версия0.1.1, уже с`"downloads"`).

2)`Import-OkExport.ps1`—`$date`задандолюбого использования (сразу после`param`); валидация:`Path`существует, этофайл(не папка), расширение`.json`; копия в`data/incoming/`; развёрнутый отчёт (источник / приземлено / sha256 / журнал / коммит).

3) SYNOPSISв шапку`Import-OkExport.ps1`: «Файл берётся: кнопка «Экспорт» в картотеке ИЛИ Console SW → Save as; Path = путь к этому файлу» (+`.EXAMPLE`).

Коммит:`fix: экспорт ОК — кнопка, downloads, Import-OkExport [TASK-0166]`.

## Осталось (за вами)

1. Скопировать в mem-OK:`scripts/Add-ExportButton.ps1`,`scripts/Import-OkExport.ps1`(+`EdgeExtension/manifest.json`, если хотите полный текст вместо патча).
2. Прогнать`pwsh scripts/Add-ExportButton.ps1`из корня mem-OK → Reload расширения → проверить кнопку «Экспорт» (скачивает`ok-export-*.json`).
3. Экспортировать и прогнать`Import-OkExport.ps1 -Path <файл>`, затем коммит.

Если пришлёте актуальный`kartoteka.js`/`kartoteka.html`— вставлю кнопку точечно прямо в них вместо патч-скрипта.

Готово — добавка к TASK-0166 исполнена, сборка зелёная (48 модулей). Отчёт.

## Сделано (добавка начальника к TASK-0166)

Все правки — в`mem-OK-prep/scripts/Import-OkExport.ps1`(перезаписан начисто):

1. `$date__ok`инициализирована ДО первого чтения—`$date__ok = Get-Date -Format "yyyy-MM-dd"`стоит сразу после`Set-StrictMode`. Заодно устранён скрытый баг прошлой версии: там была определена`$date`, а в имени файла интерполировалось неопределённое`$date__ok`(в StrictMode это упало бы). Теперь имя файла —`"${date__ok}__ok-kartoteka-export.json"`→`2026-08-21__ok-kartoteka-export.json`(скобки отсекают жадный захват имени переменной).
2. Валидация`Path`— обязан бытьФАЙЛОМ`.json`:`Test-Path -LiteralPath $Path -PathType Leaf`+ проверка расширения; при нарушении — понятная строка «Path — путь к json-файлу экспорта, не папка и не профиль».
3. SYNOPSIS в шапке— «Файл берётся: кнопка «Экспорт» в картотеке ИЛИ Console SW → Save as; Path = путь к этому файлу» +`.EXAMPLE`.
4. Отчёт— блок`СДЕЛАНО/ОСТАЛОСЬ`в выводе скрипта (источник, приземлено, sha256, журнал, коммит / слияние, движение).

Кнопка «Экспорт» (`chrome.downloads.download`),`manifest.json`с`"downloads"`и патч-скрипт`Add-ExportButton.ps1`были готовы в прошлом раунде — не тронуты.

Бонус:зафиксировал «следующее движение» как решение`d-004`в каноне`src/data/decisions.ts`и в сиде дашборда — дорога теперь видна в секции РЕШЕНИЯ.

## Осталось (следующее движение — когда 0166 вернётся)

1. Кнопка «Экспорт» → файл в Загрузки →`Import-OkExport.ps1 -Path файл`→ incoming (скрипт сам разложит, отчёт в консоль).
2. Слияние пяти карточек с картотекой ЕУ (`merge-cabinets`из mem-ok-s-admin ИЛИ починенный импорт) — имена Пятачка прописываются в общей картотеке.
3. Движение в дашборд: «экспорт-импорт замкнут, 5 карточек в ЕУ».

Коммит:`fix: экспорт ОК — кнопка, downloads, Import-OkExport [TASK-0166]`.

Добавка к TASK-0166 исполнена: $date__ok теперь инициализируется до первого чтения, валидация Path проверяет файл .json с понятной ошибкой, добавлен SYNOPSIS и отчёт сделано/осталось. Устранён скрытый баг с именем переменной. Следующее движение зафиксировано в решениях дашборда. Осталось: прогнать экспорт→импорт, слить 5 карточек с картотекой ЕУ, добавить движение в дашборд.