Сделано. Пятачок дошёл до плиты с именами в кармане — кладу отчёт.

## Пункт 1 — движение в дашборд +`logs/activity`✓

Канон — в`src/data/`, дашборд засеян теми же записями, чтобы движение было видно сразу (до компиляции):

- `src/data/moves.ts`—`m-0001`: «маркировка ОК полетела: 5 карточек, правила v07g живы» (kind`mark`, target`ok.ru · TASK-0151 PASS`).
- `src/data/decisions.ts`— решения-дорога`d-001…d-003`(см. пункты 3–5 ниже), статус «принято».
- `src/data/debug.ts`— приёмочная строка: «5 карточек (1 СОО „Лента" + 4 ПЕРСОН), дедуп живой, NAME_HINT живой, граница PERSON/COMMUNITY держит».
- `dashboard/index.html`— временный сид`window.MEMOK`зеркалит канон один в один → в ДВИЖЕНИЯХ оранжевая строка маркировки, в РЕШЕНИЯХ три принятых, в ОТЛАДКЕ зелёная приёмка. После компиляции`src/data/*.ts`сид удаляется.
- `logs/activity/moves.log`— строка`[2026-08-21] mark | ok | … | TASK-0151 PASS · дашборд`.

## Пункт 2 — экспорт картотеки →`data/incoming/`(файл-первый) ✓

- `data/incoming/README.md`— правила посадки: имя`YYYY-MM-DD__ok-kartoteka-export.json`, append, provenance, «результаты пишем СЕБЕ».
- `scripts/Import-OkExport.ps1`— одна команда:`pwsh scripts/Import-OkExport.ps1 -Path <экспорт>`. Сама переименует по конвенции (без перезаписи того же дня), посчитает SHA256, допишет строку в`logs/activity/moves.log`и подскажет коммит:`chore: картотека из расширения -> data/incoming (файл-первый)`.

## Пункты 3–5 — зафиксированы в плане (решения d-001…d-003)

- d-001OBD-сниппет (TASK-0161): раскрытие на вставке, чтение со смартфона — когда придёт черёд.
- d-002Приём волонтёров (`tasks/`) — после одиночного прогона.
- d-003Эксплуатационный дашборд — не скоро, но в плане.

## Осталось (за вами)

1. Скопировать файлы из`mem-OK-prep/`в`mem-OK`(dashboard/index.html, src/data/*.ts, logs/activity/moves.log, data/incoming/README.md, scripts/Import-OkExport.ps1).
2. Экспортировать картотеку из расширения и прогнать`Import-OkExport.ps1`, затем коммит из пункта 2.
3. Коммит движения:`feat: движение TASK-0151 в дашборд и logs/activity`.

Сборка дашборда зелёная (48 модулей), prep-файлы основную сборку не трогают.

И плита: Корпечь ждёт — а Пятачок уже несёт 5 имён, Пух несёт смыслы. По дороге, по порядку.

Пункты 1–2 выполнены: движение TASK-0151 добавлено в дашборд (src/data + сид в index.html) и в logs/activity; подготовлен экспорт картотеки в data/incoming с помощником Import-OkExport.ps1 (файл-первый, SHA256, коммит-подсказка). Пункты 3–5 зафиксированы как решения d-001–d-003. Осталось скопировать файлы в mem-OK, экспортировать картотеку и закоммитить. Сборка зелёная.